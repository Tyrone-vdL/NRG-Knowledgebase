# `wiki/sources/` — Source Summaries

One markdown file per ingested source document. Filename convention: `src-<slug>.md`.

Each file extracts the key facts from the source PDF so an LLM doesn't have to re-read the PDF every time. Frontmatter ties the summary back to the source path.

See `OPERATIONS.md` (root) for the ingest procedure.
