# `wiki/queries/` — Saved Q&A

When a query produces a useful synthesis worth keeping (especially answers that took real work, or answers likely to come up again from clients), file it here.

Filename convention: `q-<slug>.md` — e.g. `q-hebel-wall-r-value-cz6.md`.
