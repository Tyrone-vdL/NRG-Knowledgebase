# Shell scripts and systemd units run on the Linux droplet — force LF so a
# Windows checkout never commits CRLF that would break `bash`/systemd.
*.sh text eol=lf
*.mjs text eol=lf
deploy/*.service text eol=lf
deploy/*.timer text eol=lf
